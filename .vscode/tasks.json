{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run backend unit tests",
			"type": "shell",
			"command": "uv run pytest backend/tests -q",
			"group": "test"
		}
		,
		{
			"label": "Run full backend tests",
			"type": "shell",
			"command": "uv run pytest backend -q",
			"group": "test"
		}
	]
}